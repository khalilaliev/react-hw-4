import Title from "../components/Title/Title";
import { NOT_FOUND } from "../constants/page-title-text";

const NotFound = () => {
  return (
    <>
      <Title text={NOT_FOUND} />
    </>
  );
};

export default NotFound;
