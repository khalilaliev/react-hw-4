import Title from "../components/Title/Title";
import { USER_INFO } from "../constants/page-title-text";

const UserInfo = () => {
  return (
    <>
      <Title text={USER_INFO} />
    </>
  );
};

export default UserInfo;
